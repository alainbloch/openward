class Setting < ActiveRecord::Base

belongs_to :connection_page, :class_name => "Page"
belongs_to :subscribe_page,  :class_name => "Page"
belongs_to :archive_page,    :class_name => "Page"

COMMENT_EXPIRATION = [90, 60, 30, 15]

end
